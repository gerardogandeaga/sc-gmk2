/// @description get the player manager from specified index
var index = argument0;

// check if index is with in the bounds of the list
var _manager = obj_game_manager.player_manager_arr[index];
return _manager;
